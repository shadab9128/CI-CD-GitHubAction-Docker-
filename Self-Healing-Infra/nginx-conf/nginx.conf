events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location /nginx_status {
            stub_status on;
            allow all;
            access_log off;
        }

        location / {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}
